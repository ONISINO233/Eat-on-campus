spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eat_on_campus
    username: root
    password: '191011'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: onisino.eatoncampus.entity
  configuration:
    map-underscore-to-camel-case: true